NODE_ENV=development
PORT=3000

# Docker (docker-compose.ext.yml): use the redis service hostname
REDIS_URL=redis://redis:6379
# Bare metal on VM (npm run workers): use redis-server on the host
# REDIS_URL=redis://127.0.0.1:6379

# MongoDB Atlas — replace with your connection string
MONGO_URL=url
